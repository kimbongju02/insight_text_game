const express = require('express');
const router = express.Router();

router.get('/generate/init/:id', (req, res)=>{

})

router.get('load/image/story/:id', (req, res)=>{

})

router.post('generate/story',(req,res)=>{

})